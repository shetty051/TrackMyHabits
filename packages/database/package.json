{
  "name": "@trackmyhabits/database",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "prisma generate --schema=prisma/schema.postgres.prisma",
    "build:sqlite": "prisma generate --schema=prisma/schema.prisma",
    "db:generate": "prisma generate",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0"
  }
}
