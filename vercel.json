{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd apps/frontend && npx prisma generate --schema=../../packages/database/prisma/schema.postgres.prisma && npm run build",
  "outputDirectory": "apps/frontend/.next",
  "crons": [
    {
      "path": "/api/cron/rollover",
      "schedule": "0 0 * * *"
    }
  ]
}
