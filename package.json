{
  "name": "track-my-habits-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npx prisma generate --schema=packages/database/prisma/schema.postgres.prisma && npm run build --workspace=@trackmyhabits/frontend",
    "dev:frontend": "npm run dev --workspace=@trackmyhabits/frontend",
    "dev:backend": "npm run start:dev --workspace=@trackmyhabits/backend"
  }
}
